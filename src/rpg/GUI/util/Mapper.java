package rpg.GUI.util;

import com.fasterxml.jackson.databind.ObjectMapper;

//Criação de um ObjectMapper estático
public class Mapper {
    public static final ObjectMapper mapper = new ObjectMapper();
}
